name: Python Automation CI

on:
  push:
    branches:
      - main

jobs:

  run-automation:

    runs-on: windows-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v3